<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sports">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login"></activity>
        <activity android:name=".registerplayer"></activity>
        <activity android:name=".registertrainer"></activity>
        <activity android:name=".registerorganiser"></activity>
        <activity android:name=".loginuserpass"></activity>
        <activity android:name=".home"></activity>
        <activity android:name=".homeorganiser" />
        <activity android:name=".hometrainer" />
        <activity android:name=".viewprofile" />
        <activity android:name=".Addupdatetournament"></activity>
        <activity android:name=".logout" />

    </application>

</manifest>